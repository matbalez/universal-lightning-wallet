# NIP-101

## Universal Lightning Wallet

`draft` `optional`

This NIP defines Universal Lightning Wallet (ULW), a Nostr-based protocol for connecting applications to Lightning wallets with scoped permissions, wallet-enforced spending limits, BOLT11 send/receive, and BOLT12 send/receive.

ULW has two conformance profiles:

- `ULW-fluent`: a wallet that implements the ULW Connection Protocol.
- `ULW-embeddable`: a wallet SDK that lets an application create and manage a wallet for a user. A ULW-embeddable wallet MUST also be ULW-fluent.

## Motivation

Applications increasingly want Bitcoin payments as a native product primitive: profiles with reusable receive targets, tips, paid posts, paid agent requests, channel revenue, and wallet-gated actions.

NIP-47/Nostr Wallet Connect established the right basic shape: pairwise app-wallet keys, encrypted requests over relays, and scoped wallet access. ULW keeps that shape, but makes BOLT12 a first-class requirement, adds app-initiated authorization, requires wallet-enforced spending policy, defines idempotent payments, and separates the wire protocol from embedded-wallet SDK requirements.

ULW is not a wallet architecture, custody model, liquidity protocol, relay service, seed format, or stablecoin protocol.

## Dependencies

Implementations use:

- NIP-01 for Nostr events, tags, filters, and signatures.
- NIP-40 for `expiration` tags.
- NIP-44 v2 for encrypted payloads.
- NIP-42 only when a chosen relay requires authenticated access.
- BIP-321 for payment instructions.
- BOLT11 invoices.
- BOLT12 offers.

NIP-04 payloads MUST NOT be used.

## Event Kinds

This draft uses the following candidate event kinds:

| kind | name | use |
| --- | --- | --- |
| `19470` | `ULW_INFO` | replaceable wallet capability/info event |
| `39470` | `ULW_AUTH_RESPONSE` | app-initiated authorization response |
| `9470` | `ULW_REQUEST` | app request |
| `9471` | `ULW_RESPONSE` | wallet response |
| `9472` | `ULW_NOTIFICATION` | wallet notification |

Request, response, and notification events are regular stored events. Relays SHOULD store them until their `expiration` timestamp. Implementations MUST NOT rely on relays honoring expiration or deletion.

Every `ULW_REQUEST`, `ULW_RESPONSE`, and `ULW_NOTIFICATION` event MUST:

- be signed by the sender connection key;
- include exactly one `p` tag for the recipient connection key;
- include an `expiration` tag;
- include an `alt` tag;
- encrypt `content` using NIP-44 v2.

Responses MUST include an `e` tag referencing the request event.

`ULW_INFO` is an optional public replaceable event for non-user-specific service metadata. Its `content` is plaintext JSON:

```json
{
  "version": "ulw-1",
  "methods": ["get_info", "get_balance", "pay", "make_invoice", "make_offer", "lookup_payment", "list_transactions", "disconnect"],
  "payment_instructions": ["bolt11", "bolt12"],
  "authorization": ["app_initiated", "wallet_initiated"]
}
```

`ULW_INFO` MUST NOT contain user-specific wallet data. `get_info` is authoritative for a specific connection.

## Connections

A connection is one app-wallet authorization. It is identified by a pairwise app connection key and wallet connection key.

Apps and wallets SHOULD use a fresh connection key for each authorization. They MUST NOT use a user's long-term public Nostr identity key as a connection key unless the user explicitly chooses that privacy loss.

Connection material MUST include one or more relay URLs. Implementations SHOULD use at least two independent relays when practical. Senders SHOULD publish to all connection relays; receivers SHOULD subscribe on all connection relays and deduplicate events.

Relays are untrusted. They can drop, delay, reorder, duplicate, censor, and store events.

## Authorization

ULW supports app-initiated and wallet-initiated authorization.

### App-Initiated Authorization

The app generates:

- a fresh app connection keypair;
- a random `state` value with at least 128 bits of entropy;
- required commands;
- optional commands;
- a budget if any spend command is requested;
- relay URLs where it will listen for the response.

The app presents:

```text
nostr+ulw-auth://<app_pubkey>?relay=<url>&state=<random>&required=<commands>&optional=<commands>&budget=<base64url-json>&name=<app-name>
```

`relay` is REQUIRED and repeatable. `state` is REQUIRED. `required` is REQUIRED. `optional`, `budget`, and `name` are OPTIONAL, except `budget` is REQUIRED if any requested command is a spend command.

The wallet MUST show the user the requesting app, requested commands, requested budget, expiry, and relay list before approval. The wallet MAY grant fewer commands than requested. It MUST NOT grant commands the app did not request unless the user is explicitly shown them.

If approved, the wallet publishes kind `39470` encrypted to the app pubkey:

```json
{
  "type": "authorization_response",
  "version": "ulw-1",
  "state": "same-random-state",
  "approved": true,
  "relays": ["wss://relay.example"],
  "permissions": {
    "commands": ["get_balance", "pay", "make_offer"],
    "notifications": {
      "types": ["payment_received", "payment_sent"],
      "delivery": "online_service"
    }
  },
  "budget": {
    "max_amount_msat": 1000000,
    "window_seconds": 86400,
    "max_payment_msat": 250000,
    "max_fee_msat": 5000
  },
  "expires_at": 1790000000
}
```

The event author is the wallet connection pubkey. The event MUST include a `p` tag for the app pubkey, a `d` tag equal to `state`, and an `expiration` tag. The app MUST verify the event author, NIP-44 decryption, `state`, relay list, granted commands, and budget.

If rejected, the wallet MAY publish an authorization response with `approved: false` and an error object.

### Wallet-Initiated Authorization

A wallet MAY generate a NWC-like bearer URI:

```text
nostr+ulw://<wallet_pubkey>?relay=<url>&secret=<app_private_key_hex>&commands=<commands>&budget=<base64url-json>&expires_at=<unix>
```

The `secret` is spend-authority material. Anyone who sees it can use the grant until expiry or revocation, subject to wallet-enforced permissions and budget.

Apps MUST store wallet-initiated authorization secrets in protected storage.

If `commands` includes `pay`, the URI MUST include a budget.

## Permissions

Permissions are granted per connection. A wallet MUST reject any command not granted to the connection.

Defined v1 commands:

- `get_info`
- `get_balance`
- `pay`
- `make_invoice`
- `make_offer`
- `lookup_payment`
- `list_transactions`
- `disconnect`

`pay` is a spend command. Future spend commands MUST require a wallet-enforced budget unless a later specification explicitly says otherwise.

## Budgets

Spend-authorized connections MUST have a wallet-enforced budget. App-side limits are not a security boundary.

A budget object MUST include `max_amount_msat`. It MUST include either `window_seconds` or an authorization `expires_at`.

```json
{
  "max_amount_msat": 1000000,
  "window_seconds": 86400,
  "max_payment_msat": 250000,
  "max_fee_msat": 5000
}
```

`max_amount_msat` is the maximum cumulative outgoing debit for the window or lifetime. Wallets MUST count settled outgoing amount plus fees against it.

`max_payment_msat` and `max_fee_msat` are optional but RECOMMENDED. A wallet MUST reject a payment before attempting it if the amount or maximum fee can exceed the remaining budget or any per-payment limit.

Wallets MAY enforce stricter local policies.

## Request and Response Format

All encrypted plaintext MUST be UTF-8 JSON and MUST include `version: "ulw-1"`.

Request plaintext:

```json
{
  "type": "request",
  "version": "ulw-1",
  "request_id": "random-128-bit-or-larger-id",
  "method": "pay",
  "params": {},
  "created_at": 1781640775,
  "expires_at": 1781640835
}
```

Response plaintext:

```json
{
  "type": "response",
  "version": "ulw-1",
  "request_id": "same-id",
  "result_type": "pay",
  "result": {},
  "error": null,
  "created_at": 1781640776
}
```

`request_id` MUST be unique per app connection. Apps MUST reuse the same `request_id` when retrying the same logical request. Wallets MUST make spend commands idempotent by app pubkey and `request_id`. A duplicate spend request MUST NOT execute a second payment.

If event `expiration` and plaintext `expires_at` differ, the earlier expiry wins.

## Errors

When applicable, wallets MUST use these error codes:

| code | meaning |
| --- | --- |
| `BAD_REQUEST` | malformed request or invalid parameters |
| `UNAUTHORIZED` | unknown or revoked app key |
| `RESTRICTED` | command not granted |
| `QUOTA_EXCEEDED` | budget exceeded |
| `INSUFFICIENT_BALANCE` | insufficient spendable balance |
| `PAYMENT_FAILED` | payment attempted and failed |
| `NOT_FOUND` | requested record not found |
| `EXPIRED` | request expired |
| `DUPLICATE` | duplicate request cannot return prior result |
| `NOT_IMPLEMENTED` | known method not implemented |
| `UNSUPPORTED_PAYMENT_INSTRUCTION` | unsupported BIP-321 instruction |
| `UNSUPPORTED_NETWORK` | unsupported Bitcoin network |
| `RATE_LIMITED` | app is sending too many requests |
| `TEMPORARILY_UNAVAILABLE` | wallet cannot process now; app may retry |
| `INTERNAL` | wallet internal failure |
| `OTHER` | failure not covered above |

## Commands

### `get_info`

Returns wallet and connection capabilities.

Result fields:

- `alias`
- `network`: `mainnet`, `testnet`, `signet`, or `regtest`
- `wallet_pubkey`
- `methods`: granted methods for this connection
- `notifications`
- `payment_instructions`: MUST include `bolt11` and `bolt12`
- `receive.bolt12.availability`
- `budget`, if any

`methods` MUST describe this connection, not merely global wallet support.

### `get_balance`

Returns:

- `balance_msat`
- `available_msat`
- `pending_inbound_msat`
- `pending_outbound_msat`

`available_msat` SHOULD be immediately spendable balance after wallet reserves.

### `pay`

Pays a BIP-321 payment instruction.

Params:

- `payment`: REQUIRED BIP-321 URI
- `amount_msat`: REQUIRED if the selected instruction has no amount
- `max_fee_msat`: REQUIRED unless the wallet applies a stricter connection policy
- `payer_note`: OPTIONAL
- `timeout_seconds`: OPTIONAL
- `metadata`: OPTIONAL

ULW v1 wallets MUST support BIP-321 URIs containing:

- `lightning`: BOLT11 invoice
- `lno`: BOLT12 offer

Wallets MAY accept bare BOLT11 or BOLT12 strings, but MUST normalize them internally as BIP-321 payment instructions.

Other BIP-321 instructions are not required. Unsupported instructions MUST be rejected before payment.

For BOLT12 offers, the wallet MUST perform invoice request and invoice retrieval internally.

If `payer_note` is present and the selected payment instruction supports payer-provided messages, the wallet MUST deliver it. If the wallet cannot deliver a non-empty `payer_note`, it MUST reject the request before payment.

Wallets MUST validate network compatibility and MUST NOT exceed `max_fee_msat`, connection budget, or local policy.

Result fields:

- `payment_id`
- `state`
- `instruction_type`: `bolt11` or `bolt12`
- `amount_msat`
- `fees_paid_msat`
- `payment_hash`, if available
- `preimage`, if available
- `created_at`
- `settled_at`, if settled

### `make_invoice`

Creates a BOLT11 invoice.

Params:

- `amount_msat`
- `description` or `description_hash`
- `expiry_seconds`
- `metadata`

Result fields:

- `payment_id`
- `bip321`: MUST contain a `lightning` instruction
- `invoice`: raw BOLT11 invoice, OPTIONAL
- `payment_hash`
- `amount_msat`
- `created_at`
- `expires_at`
- `state`

### `make_offer`

Creates or returns a BOLT12 offer usable as a receive target.

Params:

- `amount_msat`: OPTIONAL; omitted or null requests a variable-amount offer
- `description`
- `issuer`
- `single_use`
- `expires_at`
- `metadata`

Wallets MUST support reusable variable-amount offers. Wallets MAY return an existing offer matching the requested parameters.

Result fields:

- `offer_id`
- `bip321`: MUST contain an `lno` instruction
- `offer`: raw BOLT12 offer, OPTIONAL
- `amount_msat`
- `single_use`
- `availability`
- `created_at`
- `expires_at`

A wallet claiming ULW BOLT12 receive compliance MUST keep the offer payable while the user-facing application is offline. Valid `availability` values are:

- `always_online`: a wallet service, node, bridge, or hosted component can answer BOLT12 invoice requests and receive payments.
- `async_payments`: async payments or a protocol-equivalent LSP/static-invoice-server/onion-mailbox design can receive while the app is offline.

Interactive-only BOLT12 receive is not ULW compliant.

### `lookup_payment`

Looks up a payment by `payment_id`, `payment_hash`, invoice, or offer id.

Result fields:

- `payment_id`
- `type`: `incoming` or `outgoing`
- `state`
- `instruction_type`
- `amount_msat`
- `fees_paid_msat`
- `payment_hash`, if available
- `preimage`, if available
- `payer_note`, if available
- `created_at`
- `settled_at`, if settled
- `metadata`

### `list_transactions`

Lists transactions visible to this connection.

Params:

- `from`
- `until`
- `limit`
- `cursor`
- `type`: `incoming`, `outgoing`, or omitted

Wallets SHOULD return newest first. Wallets SHOULD use opaque cursors. `limit` MUST be capped at 20 unless a higher limit is explicitly negotiated.

Transaction records use the same fields as `lookup_payment`. They MUST include inbound and outbound payment message fields, including `payer_note`, when available.

### `disconnect`

Revokes the current app connection.

After revocation, the wallet MUST reject requests from the revoked app key even if relays continue to deliver old events. Relay deletion MUST NOT be relied on for security.

## Payment States

Payment records MUST use one of:

- `pending`
- `settled`
- `failed`
- `expired`
- `canceled`

## Notifications

Notifications are optional state-change hints. Apps MUST NOT require notifications for correctness; they must reconcile with `lookup_payment` or `list_transactions`.

Defined notification types:

- `payment_received`
- `payment_sent`
- `authorization_changed`

Notification plaintext:

```json
{
  "type": "notification",
  "version": "ulw-1",
  "notification_id": "wallet-stable-id",
  "notification_type": "payment_received",
  "notification": {}
}
```

Wallets MUST NOT advertise real-time notification support unless some wallet service can publish notifications while the user-facing app is offline. Mobile-only wallets MAY advertise deferred notifications.

## Metadata

`metadata` fields are optional opaque JSON objects. Apps MUST NOT place secrets, access tokens, seed material, or Nostr identity keys in metadata.

Wallets MAY store metadata with payment records. Wallets MUST reject or drop metadata larger than 4096 UTF-8 bytes.

Wallets and apps MUST sanitize invoice descriptions, offer descriptions, payer notes, and metadata before display.

## ULW-Embeddable Wallet SDK Profile

This profile is not the Nostr wire protocol. It defines the minimum local SDK surface for wallet providers that allow applications to create wallets for users.

A ULW-embeddable wallet MUST also be ULW-fluent.

An embeddable wallet SDK MUST expose semantic equivalents of:

```text
create_wallet(options) -> WalletHandle
open_wallet(wallet_id, options) -> WalletHandle
export_recovery_material(wallet_id, options) -> RecoveryMaterial
restore_wallet(recovery_material, options) -> WalletHandle
delete_wallet(wallet_id, options) -> DeleteResult
```

`WalletHandle` MUST expose the ULW command surface for the embedded wallet.

The SDK MUST:

- declare supported runtimes;
- create a wallet without requiring the user to leave the host application;
- persist wallet state across restarts and device reboots;
- protect wallet secrets with the strongest storage available in the runtime;
- fail closed or require explicit insecure-mode opt-in if acceptable storage is unavailable;
- support explicit recovery-material export after user action;
- support same-provider restore in another provider-supported client or runtime;
- restore to a spendable wallet, including provider-specific Lightning state needed to spend funds;
- avoid exposing plaintext seed or spend authority during ordinary wallet creation;
- never log recovery material;
- never send recovery material over the ULW Connection Protocol.

Seed-only on-chain recovery and force-close-only recovery are not sufficient for ULW embedded-wallet portability. Cross-provider wallet portability is not required.

## Non-Goals

ULW v1 does not define:

- NIP-04 compatibility;
- on-chain send or receive;
- LNURL or Lightning Address requirements;
- keysend;
- hold invoices;
- multi-pay;
- stablecoins;
- remote signing;
- arbitrary message signing;
- seed backup over Nostr;
- required default relays;
- a wallet discovery registry.

## Security Considerations

ULW grants are bearer capabilities. Possession of the app connection private key is sufficient to exercise the grant until expiry or revocation, subject to wallet-enforced permissions and budgets.

NIP-44 does not provide forward secrecy. Compromise of a connection private key can decrypt past stored messages for that connection. Implementations SHOULD rotate connection keys and use short expiry for spend permissions.

Relays see metadata: event kinds, timestamps, authors, recipients in tags, event sizes, and network-level information. ULW does not hide app-wallet relationship metadata from relays.

Budgets MUST be enforced by the wallet. App-side checks are only UX.

Spend commands MUST be idempotent. Without idempotency, relay replay and app retry can duplicate payments.

Wallets MUST validate all BOLT11 and BOLT12 objects using Lightning-layer rules before payment. ULW does not weaken Lightning validation.

Recovery material is spend-authority material. It MUST NOT be exposed to agents, renderer-only contexts, logs, channel messages, or ULW wire messages.

## Rationale

BIP-321 is used so ULW does not invent another payment-instruction wrapper. BOLT11 and BOLT12 remain opaque Lightning objects at the ULW layer.

BOLT12 is required because applications need reusable public receive targets. A wallet that can only create interactive invoices cannot satisfy this requirement.

Persistent events with expiration are used instead of ephemeral-only events so offline apps can recover responses and notifications. Correctness still comes from wallet state and reconciliation, not relay retention.

Pairwise connection keys are used to avoid linking app-wallet relationships to a user's public Nostr identity.

Wallet-enforced budgets are required because the app may be compromised or malicious.

The embedded SDK profile is separate because programmatic wallet creation is a product/SDK concern, not a Nostr wire-message concern.

## Compatibility

ULW is not backwards compatible with NIP-47. Bridges may translate between NIP-47 and ULW where semantics overlap, but ULW implementations MUST NOT inherit NIP-04 fallback or weak budget semantics.

## References

- NIP-01: https://github.com/nostr-protocol/nips/blob/master/01.md
- NIP-40: https://github.com/nostr-protocol/nips/blob/master/40.md
- NIP-42: https://github.com/nostr-protocol/nips/blob/master/42.md
- NIP-44: https://github.com/nostr-protocol/nips/blob/master/44.md
- NIP-47: https://github.com/nostr-protocol/nips/blob/master/47.md
- BIP-321: https://github.com/bitcoin/bips/blob/master/bip-0321.mediawiki
- BIP-353: https://github.com/bitcoin/bips/blob/master/bip-0353.mediawiki
- BOLT11: https://github.com/lightning/bolts/blob/master/11-payment-encoding.md
- BOLT12: https://github.com/lightning/bolts/blob/master/12-offer-encoding.md
